# Mixa web

Next.js frontend. Setup instructions are in the root README.

```
src/app/page.tsx   Analyze screen
src/lib/types.ts   API contract (mirrors api/src/mixa/schemas.py)
src/lib/api.ts     API client (NEXT_PUBLIC_USE_MOCK=1 -> mock data)
src/lib/mock.ts    example response for UI work without the API
```
